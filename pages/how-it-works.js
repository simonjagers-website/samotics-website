/* ============================================================
   SAMOTICS HOW IT WORKS PAGE — JavaScript
   Paste into Bricks Code Element → JavaScript tab
   ============================================================ */

(function () {
  'use strict';

  /* Ensure skip-link target exists */
  var main = document.getElementById('main-content');
  if (main) main.setAttribute('tabindex', '-1');
})();
